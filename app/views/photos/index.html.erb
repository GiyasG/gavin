<% @photos.each do |photo|%>
    <% if photo.authority_id.nil? and photo.project_id.nil? and photo.paper_id.nil? %>
      <%= image_tag url_for(:controller => "photos", :action => "show", :id=>photo.id), class: "col-md-6"%>
    <% end%>
<% end%>
